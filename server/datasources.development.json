{
  "db": {
    "name": "db",
    "host": "${ASSISTENCIAMOB_DB_HOST}",
    "port": "${ASSISTENCIAMOB_DB_PORT}",
    "database": "${ASSISTENCIAMOB_DB_NAME}",
    "password": "${ASSISTENCIAMOB_DB_PASSWORD}",
    "user": "${ASSISTENCIAMOB_DB_USER}",
    "connector": "mysql"
  }
}
